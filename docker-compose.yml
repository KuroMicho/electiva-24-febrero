services:
  mongodb:
    image: mongo:latest
    container_name: mongo-task-manager
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    restart: always

volumes:
  mongo-data: